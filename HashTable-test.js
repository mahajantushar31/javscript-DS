// Entry Basic Node structure
function Entry(K,v){
    this.key=k;
    this.value=v;
    this.next=null;
}

function HashTable(s){
    this.size=s;
    this.table=new Array(this.size);

    HashTable.pro
}